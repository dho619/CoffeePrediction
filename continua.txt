PASSAR O LOGIN E LOGOUT PARA O CONTEXTO, PORQUE SE EU LOGAR OU DESLOGAR O CONTEXTO TEM 
QUE ALTERAR TBM...
isso pode trocar, ta apenas para saber quem ta logado

function Drawer({navigation}) {
    const [user, setUser] = useState({});
    const { user: loggedUser} = useContext(Context);

    useEffect(() => {
        setUser(loggedUser);
        console.log(loggedUser.name)
    }, [isSignedIn()]);

referencia: https://www.youtube.com/watch?v=AClyxTbfI08

criar tela de perfil e editar perfil
adicionar opçao de contatos para adicionar quando cadastrar conta
tela para exibir, editar e apagar áreas


Coisa para se ver se vai fazer e se for como fazer:

- fazer conexão com o google
- Localização como vai ser e como pegar (uma opção: Pegar localização quando cadastra usario e criar uma area, sera a padrao em de novas analises)
- Tipos, deixa usuario cadastrar? Provavelmente melhor listar tds tipos